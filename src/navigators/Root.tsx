import React from 'react';
import {OnboardingNavigator} from './OnboardingNavigator';

export const Root = () => {
  // do the logic that has the access to the authentication state and
  // make the navigation based on that
  return <OnboardingNavigator />;
};
